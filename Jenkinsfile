pipeline {
    agent { dockerfile true }
    stages {
        stage('build') {
            steps {
                sh 'ansible --version'
                // sh 'sleep 100000s'
            }
        }
    }
}